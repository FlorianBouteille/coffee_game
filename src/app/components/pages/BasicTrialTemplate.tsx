type validateTypes = 
{
    validateTrial : (n : number) => void,
    nextTrial : number, 
    hints : Array<string>
}

export function BasicTrialTemplate({validateTrial, nextTrial, hints} : validateTypes)
{
    
}